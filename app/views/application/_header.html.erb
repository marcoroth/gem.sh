<header class="sticky h-12 px-4 top-0 z-50 w-full backdrop-blur flex-none border-b border-slate-900/10 bg-white/90 supports-backdrop-blur:bg-white/60">
  <nav class="flex items-center justify-between content-center h-full" aria-label="Global">
    <div class="flex lg:flex-1">
      <a href="<%= root_path %>" class="-m-1.5 p-1.5">
        <span class="sr-only">gem.sh</span>
        <%= inline_svg_tag "logo.svg", class: '-m-1.5 p-1.5' %>
      </a>
    </div>
    <div class="flex gap-x-4 lg:gap-x-6">
      <a href="<%= root_path %>" class="text-sm font-semibold leading-6 text-gray-900">Home</a>
      <a href="<%= gems_path %>" class="text-sm font-semibold leading-6 text-gray-900">Gems</a>
      <a href="<%= types_path %>" class="text-sm font-semibold leading-6 text-gray-900">Types</a>
      <a href="<%= docs_path %>" class="text-sm font-semibold leading-6 text-gray-900">Docs</a>
      <a href="<%= community_path %>" class="text-sm font-semibold leading-6 text-gray-900">Community</a>
    </div>
  </nav>
</header>
