<div class="grid grid-cols-4 gap-12">
  <div class="col-span-3">
    <%= render partial: "breadcrumb" %>

    <%= render Layout::Heading.new(title: @namespace) %>

    <div class="grid grid-cols-4 gap-x-8">

    </div>
  </div>

  <div class="col-span-1">
    <%= render ModulesList.new(modules: @namespace.included_modules, title: "Included Modules", gem: @gem) if @namespace.included_modules %>
    <%= render ModulesList.new(modules: @namespace.extended_modules, title: "Extended Modules", gem: @gem) if @namespace.extended_modules %>
    <%= render ClassMethodsList.new(class_methods: @class_methods, gem: @gem) %>
    <%= render InstanceMethodsList.new(instance_methods: @instance_methods, gem: @gem) %>
    <%= render ClassesList.new(classes: @classes, gem: @gem) %>
  </div>
</div>
